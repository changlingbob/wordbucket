let COMMAND = '$';
let BRACE = ['{', '}'];
const setCommand = (command) => {
    COMMAND = command;
};
const setBrace = (braces) => {
    BRACE = braces;
};
export const VARS = {
    BRACE,
    COMMAND,
    setBrace,
    setCommand,
};
//# sourceMappingURL=vars.js.map